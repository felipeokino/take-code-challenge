
import Icon from '../Icon';

import { ProfileIcon } from './Profile.styles';

export default function Profile() {
  return (
    <ProfileIcon>
      <Icon icon='profile' isButton={false} />
    </ProfileIcon>
  );
}